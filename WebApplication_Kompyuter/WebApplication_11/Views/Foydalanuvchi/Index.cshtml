@model IEnumerable<WebApplication_11.Models.Foydalanuvchi>

@{
    ViewBag.Title = "Index";
}

<h2 style ="text-align-last:center">Foydalanuvchilar</h2>

<p>
    @Html.ActionLink("Yangi qo'shish", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Userlar.Login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FISH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Uy_manzili)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tel_nomeri)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Userlar.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FISH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Uy_manzili)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tel_nomeri)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
