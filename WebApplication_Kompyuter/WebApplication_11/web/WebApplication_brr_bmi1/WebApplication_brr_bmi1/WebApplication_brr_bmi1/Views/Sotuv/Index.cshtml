@model IEnumerable<WebApplication_brr_bmi1.Models.sotuv>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tulov.Tulov_turi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Xaridor.Familiya)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Xonadon.Narxi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tulov.Tulov_turi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Xaridor.Familiya)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Xonadon.Narxi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
