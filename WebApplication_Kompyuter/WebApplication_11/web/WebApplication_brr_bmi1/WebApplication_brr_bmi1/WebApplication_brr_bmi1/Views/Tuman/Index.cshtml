@model IEnumerable<WebApplication_brr_bmi1.Models.Tuman>

@{
    ViewBag.Title = "Index";
}

<h2>Tuman</h2>

<p>
    @Html.ActionLink("Yangi qo'shish", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Viloyat.Viloyat_nomi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tuman_nomi)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <h2>@item.Tuman_nomi</h2>
                @*@Html.DisplayFor(modelItem => item.Viloyat.Viloyat_nomi)*@
            </td>
            <td>
                <h2>@item.Viloyat.Viloyat_nomi</h2>
                @*@Html.DisplayFor(modelItem => item.Tuman_nomi)*@
            </td>
            <td>
                @Html.ActionLink("O'zgartirish", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Ma'lumotlar'", "Details", new { id = item.Id }) |
                @Html.ActionLink("O'chirish", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
