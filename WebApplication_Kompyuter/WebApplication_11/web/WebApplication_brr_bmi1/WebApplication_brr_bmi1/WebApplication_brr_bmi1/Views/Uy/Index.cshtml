@model IEnumerable<WebApplication_brr_bmi1.Models.Uy>

@{
    ViewBag.Title = "Index";
}

<h2>Uylar</h2>

<p>
    @Html.ActionLink("Yangi qo'shish", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tuman.Tuman_nomi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Manzil)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tuman.Tuman_nomi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Manzil)
            </td>
            <td>
                @Html.ActionLink("O'zgartirish", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Ma'lumotlar'", "Details", new { id = item.Id }) |
                @Html.ActionLink("O'chirish", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
