@model IEnumerable<WebApplication_brr_bmi1.Models.Viloyat>

@{
    ViewBag.Title = "Index";
}

<h2>Viloyat</h2>

<p>
    @Html.ActionLink("Yangi qo'shish", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Viloyat_nomi)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Viloyat_nomi)
            </td>
            <td>
                @Html.ActionLink("O'zgartirish", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Ma'lumotlar'", "Details", new { id = item.Id }) |
                @Html.ActionLink("O'chirish", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
